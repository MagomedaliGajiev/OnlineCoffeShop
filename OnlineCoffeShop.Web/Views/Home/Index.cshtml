@model IEnumerable<OnlineCoffeShop.Web.Models.Product>
@{
    ViewData["Title"] = "Товары";
}

@foreach (var product in Model)
{
    <div>
        <div>@product.Id</div>
        <div>@product.Name</div>
        <div>@product.Cost</div>
    </div>
    <br />
}

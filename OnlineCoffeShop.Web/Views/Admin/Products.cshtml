@model IReadOnlyList<Product>
@{
    ViewData["Title"] = "Товары";
    Layout = "_AdminLayout";
}
<div class="admin-head" style="display:flex;justify-content:space-between;align-items:center">
    <h1 class="admin-title">Товары</h1>
    <a class="btn btn-cta" asp-controller="Admin" asp-action="ProductCreate">+ Добавить товар</a>
</div>

<table class="admin-table" style="width:100%;border-collapse:collapse;margin-top:16px">
    <thead>
    <tr>
        <th style="text-align:left">Название</th>
        <th style="text-align:left">Категория</th>
        <th style="text-align:right">Цена</th>
        <th style="text-align:right">Действия</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var p in Model)
    {
        <tr>
            <td>@p.Name</td>
            <td>@p.Category.DisplayName()</td>
            <td style="text-align:right">@p.Price.ToString("N0") ₽</td>
            <td style="text-align:right">
                <a asp-action="ProductDetails" asp-route-id="@p.Id">Подробно</a>
                <a asp-action="ProductEdit" asp-route-id="@p.Id">Редактировать</a>
                <form asp-action="ProductDelete" asp-route-id="@p.Id" method="post"
                      style="display:inline"
                      onsubmit="return confirm('Удалить товар «@p.Name»?');">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn-link-danger">Удалить</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>